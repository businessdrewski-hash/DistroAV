# Branch note

This package is intended for a clean `receiver-clock-downstream-diagnostics` branch created from the known-good receiver-clock branch.

Do not delete the DistroAV source tree. This package is an overlay, not a full repository replacement. The workflow relies on the repository's existing CMake files, submodules, build scripts, and local GitHub actions.
